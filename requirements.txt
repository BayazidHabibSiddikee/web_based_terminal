# ══ AVR Control Plane — Python Dependencies ══
#
# OPTIONAL (for embedded Qt window instead of browser):
PySide6>=6.5.0
#
# ── Everything else uses Python stdlib ──
# http.server, subprocess, shutil, threading,
# signal, os, sys, platform, urllib.parse
#
# ── AVR toolchain (NOT pip — use OS package manager) ──
# See install scripts:
#   install-apt.sh      → Ubuntu/Debian/Mint
#   install-dnf.sh      → Fedora/RHEL
#   install-pacman.sh   → Arch Linux
#   install.bat         → Windows
#   install-termux.sh   → Android Termux